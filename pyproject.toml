[project]
name = "lispr-flow"
version = "0.0.0"
description = "A lightweight Ubuntu-first desktop dictation app."
requires-python = ">=3.10"
dependencies = ["PySide6>=6.8,<7"]

[tool.pyside6-project]
files = ["main.py"]
